import packageJson from '../../package.json'

export const title = '키보토스'

export const subtitle = '어드민 서비스'

export const version = packageJson.version

export const paths = {
  private: '/statistics',
  public: '/login',
}
